[project]
name = "tokenizer-explorer"
version = "0.1.0"
description = "Tokenizer exploration and analysis tool"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.10.0",
]

[tool.ruff]
line-length = 100
